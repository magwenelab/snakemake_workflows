* "config/samtools_coverage_table.csv" is a CSV file like so:

```
sample,bam
SampleName,path/to/sample.bam
```

This is the same as the mosdepth workflow and hence you can use the same file for both.

